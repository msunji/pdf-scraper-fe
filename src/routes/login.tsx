import { auth } from '../firebase/firebase';
import { useState } from 'react';

function Login() {
    return (
        <>
            <h1>Login to App</h1>
            
        </>
    )
}

export default Login;